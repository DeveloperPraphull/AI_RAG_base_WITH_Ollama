apiVersion: batch/v1
kind: Job
metadata:
  name: ingest-job
  namespace: rag-system

spec:
  template:
    spec:
      containers:
      - name: ingest

        image: rag-api:latest
        imagePullPolicy: IfNotPresent

        command:
        - python
        - load_data.py

      restartPolicy: Never